# 栈  

# 主要介绍stl的stack和单调栈  
栈的特点是**先进后出**,理解栈有利于理解递归的实现,在程序中递归就是栈来实现的,涉及dfs,bfs,回溯等知识  
栈需要空间储存,如果栈的深度太大,或者进栈的数组太大,则会超出系统为栈分配的空间,就会爆栈,导致栈溢出,也是递归的主要问题,递归不能太深(你之前或许以为是超时了,不对是爆栈了)   
我们实现时,一般是用库(别人写好的)中的stack,但有些情况要自己手写,自己写的性能更好;  
栈能解决很多文本输入问题,比如输入错误了,要删除上一个节点,这时只需要进行出栈操作就行;  